PORT=5001
MONGODB_URI=mongodb://localhost:27017/expense-tracker
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_EXPIRES_IN=7d
NODE_ENV=development

# Optional — AI features (receipt scanning, auto-categorisation)
# Get your key at https://console.anthropic.com
# Leave blank to run in demo mode (keyword-based fallback, no API calls)
ANTHROPIC_API_KEY=
